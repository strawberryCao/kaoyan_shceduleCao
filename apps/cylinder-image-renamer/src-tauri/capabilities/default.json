{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the cylinder image renamer window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
